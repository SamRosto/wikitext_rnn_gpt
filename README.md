# Общее описание задачи
Нужно решить задачу предсказания следующего слова — обучить нейросеть, которая по началу фразы предсказывает наиболее вероятное следующее слово. Эта задача лежит в основе подсказок при наборе текста на клавиатуре смартфона, автодополнений в поисковых строках и подсказок в мессенджерах.

# Формальная задача
Поэтапное описание:
Подготовить текстовый датасет WikiText-2 для обучения языковой модели.
Реализовать и обучить LSTM-модель для предсказания следующего токена.
Провести эксперимент: заменить LSTM на GRU и сравнить результаты.
Замерить качество предобученного трансформера distilgpt2 на тех же данных.
Сравнить все модели и дать рекомендации: какую модель стоит использовать и почему.

# Данные: WikiText-2
WikiText-2 — стандартный бенчмарк для языковых моделей на основе статей Википедии. Датасет удобен тем, что:
содержит качественные тексты на английском языке;
имеет стандартное разделение на train/val/test;
широко используется в научных работах для сравнения моделей.

# Метрика: perplexity
Качество языковой модели измеряется метрикой perplexity (перплексия). Perplexity показывает, насколько модель «удивляется» реальным текстам: чем ниже значение, тем лучше модель предсказывает следующее слово.
Perplexity вычисляется напрямую из кросс-энтропийной функции потерь:
$PPL = exp(CrossEntropyLoss)$
Поскольку вы уже работали с ```CrossEntropyLoss``` в уроках спринта, вычислить perplexity будет просто — это всего лишь экспонента от значения лосса.
Интерпретация: perplexity можно понимать как среднее число вариантов, между которыми выбирает модель. Если perplexity = 100, модель в среднем «сомневается» между 100 словами. Если perplexity = 10 — между 10.

```python
class LSTMModel(torch.nn.Module):
    def __init__(self, vocab_size, emb_dim, hidden_dim, num_layers):
        super().__init__()
        self.emb = torch.nn.Embedding(num_embeddings=vocab_size, embedding_dim=emb_dim)
        self.lstm = torch.nn.LSTM(input_size=emb_dim, hidden_size=hidden_dim, num_layers=num_layers, batch_first=True)
        self.fc = torch.nn.Linear(in_features=hidden_dim, out_features=vocab_size)
```
* Пояснения
```python
self.emb = nn.Embedding(vocab_size, emb_dim)
```
Создаёт таблицу эмбеддингов размером vocab_size × emb_dim (например 39274 × 128). Это просто большая матрица — каждая строка это вектор для одного слова. Когда приходит индекс токена 42, она возвращает строку номер 42.

```python
self.lstm = nn.LSTM(emb_dim, hidden_dim, num_layers, batch_first=True)
```
Создаёт LSTM с входом размером 128 (выход эмбеддинга), скрытым состоянием 256, двумя слоями. batch_first=True первое измерение тензора — это batch, а не seq_len 

```python
self.fc = nn.Linear(hidden_dim, vocab_size)
```
Создаёт матрицу 256 × 39274. На каждой позиции последовательности берёт вектор 256 из LSTM и превращает его в 39274 логитов — по одному на каждое слово словаря.
